@model IEnumerable<PendalePharmacy.Models.Drugs>

@{
    ViewBag.Title = "Index";
}

@if (User.Identity.Name != "sumeetjudge@gmail.com")
{
<h2>To manage this page login with Admin Credtianials : Username = "sumeetjudge@gmail.com" Password="Brampton007!"</h2>
}

@if (User.Identity.Name == "sumeetjudge@gmail.com")
{
    <div style="padding:10px">


        <a class="btn btn-primary" href="@Url.Action("Create")">Create New</a>
    </div>
} 

<table class="table table-striped table-hover">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.drugName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.comments)
        </th>
@if (User.Identity.Name == "sumeetjudge@gmail.com")
{
        <th>
            Manage
        </th>
}
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.drugName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.comments)
        </td>
@if (User.Identity.Name == "sumeetjudge@gmail.com")
{
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.DrugsID }) |
            @Html.ActionLink("Details", "Details", new { id = item.DrugsID }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.DrugsID })
        </td>
}
    </tr>
}

</table>
